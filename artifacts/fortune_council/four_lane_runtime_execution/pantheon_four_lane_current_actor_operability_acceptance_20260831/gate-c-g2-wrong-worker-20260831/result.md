# Gate C G2 wrong-worker result

`G2_GREEN`。baseline `1 passed in 0.03s`，test-only strengthening 後 target `1 passed in 0.14s`。wrong formal-job lane 以 `ValueError: formal review identity` 拒絕；tmp_path 全檔案 bytes 不變、`_atomic_write_json` spy 為零，且不存在 editorial stage。provider/service/network/production mutation 均為 0。
