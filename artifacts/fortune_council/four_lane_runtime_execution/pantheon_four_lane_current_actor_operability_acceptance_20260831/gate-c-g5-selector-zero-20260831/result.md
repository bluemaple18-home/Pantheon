# G5 selector zero — GREEN

Exact node rejects `not found`; queue/state/ledger tree bytes are unchanged. `_atomic_write_json`, publish transaction, and `subprocess.run` spies are all zero. Baseline `1 passed in 0.26s`; target `1 passed in 0.25s`.
