# G6 selector many — GREEN

Exact node rejects `must name exactly one`; queue/state/ledger tree bytes are unchanged. `_atomic_write_json`, publish transaction, and `subprocess.run` spies are all zero. Baseline and target both `1 passed in 0.24s`.
