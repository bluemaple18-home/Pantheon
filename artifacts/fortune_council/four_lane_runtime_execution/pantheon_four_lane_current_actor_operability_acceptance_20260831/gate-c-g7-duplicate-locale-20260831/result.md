# G7 duplicate locale — GREEN

Exact node rejects `coverage`; all possible multilingual local persistence (`_atomic_write_json`, `_atomic_write_bytes`, subprocess) is spied to zero and tmp fixture tree bytes are unchanged. Baseline and target both `1 passed in 0.14s`.
