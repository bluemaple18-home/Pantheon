# G8 ledger lifecycle conflict — GREEN

Exact node rejects `ledger lifecycle conflict`; queue/state/ledger tree bytes are unchanged. `_atomic_write_json`, create/translation publisher entrypoints, and `subprocess.run` spies are zero. Baseline and target both `1 passed in 0.26s`.
