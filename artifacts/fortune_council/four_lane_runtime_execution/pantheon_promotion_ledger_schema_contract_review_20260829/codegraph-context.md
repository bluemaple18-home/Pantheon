# CodeGraph Task-Semantic Query

Query: `Pantheon content runtime promotion ledger schema contract repair canonicalizer formal producer schemas plan-only idempotence`

Returned entry points:

- `PromotionError` in `scripts/pantheon_content_runtime_promotion.py`
- `PromotionRequest` in `scripts/pantheon_content_runtime_promotion.py`
- `PromotionCrashStop` in `scripts/pantheon_content_runtime_promotion.py`

Reviewer decision: CodeGraph provided only high-level promotion runtime entry points for this query, so the review used current source/test diff, formal publisher producer source, and saved command evidence as the source of truth for the schema contract.
